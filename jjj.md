El patch cubre:

styles/globals.css (nuevo contenido)

components/shared/storeinfo.tsx (reemplazos de color inline — ya lo preparé anteriormente)

components/shared/ProductCard.tsx (textos y badges)

components/shared/ProductTable.tsx (celdas, badges)

components/back-button.tsx (botón)

components/ui/search-bar.tsx (icon y ring)

app/producto/[id]/page.tsx (precios)

components/Loading.tsx (skeleton gradient + uses variables)